{
  "name": "get-involved",
  "private": true,
  "version": "0.0.0",
  "description": "Static page that opens a prepared email to the German finance minister.",
  "license": "MIT",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.0"
  }
}
